# AI Drive v0.4.6

- Installs the allow-listed WebDAV compatibility patch through the v0.4.5 updater.
- Makes `/personal/` resolve to the Agent personal drive.
- Makes `/department/` resolve to the `智能体` shared department.
- Retains all Agent `write`, `upload`, and `rename` fixes.
