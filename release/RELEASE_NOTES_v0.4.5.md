# AI Drive v0.4.5

- Includes the Agent `write` and `rename` fixes from v0.4.4.
- Keeps the online-update package compatible with installations running the v0.4.3 updater.
- Docker and SPK distributions include `/personal/` and `/department/` WebDAV aliases.
- The updater now accepts one explicitly allow-listed WebDAV compatibility file for future releases; arbitrary paths remain rejected.
