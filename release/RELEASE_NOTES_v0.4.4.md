# AI Drive v0.4.4

- Fix `write` creating a new file being mistaken for its parent folder.
- Accept `name`, `newName`, `to`, `dest`, or `destination` for `rename`.
- Add stable WebDAV aliases: `/personal/` and `/department/`.
- Document action parameters in the `capabilities` response.
- Include the WebDAV compatibility patch in online-update packages.
